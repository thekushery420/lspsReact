import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  Grid: {
    margin: "0 auto",
    marginBottom: "20px",
  },
  root: {
    margin: "1.5em 0",
  },
  media: {
    height: "160px",
    width: "300px",

    margin: "auto",
  },
  cardContent: {
    textAlign: "center",
  },
}));
